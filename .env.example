COMPOSE_PROJECT_NAME=wsa-enterprise

# --- Production (M12.1) ---
# Use .env.production.example as the production template.
# DOMAIN=app.example.com
# CERTBOT_EMAIL=ops@example.com
# POSTGRES_PASSWORD=REPLACE_WITH_STRONG_UNIQUE_SECRET
# VITE_API_URL=https://app.example.com/api/v1

# --- GHCR deploy (M12.2) ---
# IMAGE_TAG=main
# GHCR_IMAGE_PREFIX=ghcr.io/wsa-platform

# Local development defaults only. Production must set its own secret values.
POSTGRES_DB=wsa_enterprise
POSTGRES_USER=wsa
POSTGRES_PASSWORD=REPLACE_WITH_LOCAL_DEVELOPMENT_SECRET
