# Archived Laravel skeleton workflows

These files were copied from the default Laravel application template and are **superseded** by the repository root CI at `.github/workflows/ci.yml`.

They are retained for reference only and are not executed by GitHub Actions.
