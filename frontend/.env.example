# Optional: override API base URL for local Vite dev (default /api/v1 uses same-origin via Nginx on :8081)
# VITE_API_URL=http://localhost:8081/api/v1

# Show demo login defaults and hint (true in local dev; false in production Docker builds)
# VITE_SHOW_DEMO_LOGIN=true
